services:
  redis:
    image: redis:alpine
    restart: unless-stopped
    network_mode: host
    command: redis-server --requirepass ${REDIS_PASSWORD}
    env_file:
      - .env

  bot:
    build: .
    network_mode: host
    restart: always

    depends_on:
      - redis
    env_file:
      - .env
